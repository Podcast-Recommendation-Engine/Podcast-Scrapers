version: '3.8'

services:
  podcast-scraper:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: podcast-scraper
    volumes:
      - ../data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - podcast-network

networks:
  podcast-network:
    driver: bridge